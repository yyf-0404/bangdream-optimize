# 前端统一接入与验收记录

## 当前状态（2026-09-14）

- 已移除国服专用完整账号导入的页面接入、服务端路由与部署配置；保留 `crates/bangdream-account` 源码及工作区成员，供后续新导入复用；国服与其他服务器统一保留 Bestdori 主乐队公开资料导入，配置粘贴导入保持可用。
- 用户在完成 diff 整理后授权提交。本轮按确认稿与素材、正式功能接入、部署与验收文档三组组织本地提交，未发布或推送远端。
- 已修复档案弹窗行内服务器图标失去尺寸的问题；上一轮文本树检查漏掉实际视觉错误。本轮六种弹窗状态的宽窄屏截图复查见 [2026-09-14 弹窗复查](frontend-acceptance-2026-09-14.md)，不将逻辑测试通过等同于全量视觉通过。
- 活动类型改为静态文字，自定义活动通过“更改类型”打开六项单选面板。恢复旧版六类活动多选筛选，“全选”始终显示该名称，点击切换全选／全不选；筛选只改变候选，不更改当前配置。

当前前端为版本 49 的统一五页界面；顶部增加导入到当前档案的快捷入口，下载桌面端移到侧栏。活动候选列表右侧显示与筛选一致的类型简称，长名称允许换行。下文保留各阶段历史，不将早期方案视为现行规范。

v49 逐页查看宽窄屏和主要弹窗，修复矮窗口侧栏、歌曲选择与结果详情样式、结果菜单裁切及桌面档案导出绕过原生保存的问题。101 项前端测试与桌面开发构建通过；原生桌面窗口和安装包下载仍未实机验收。实际覆盖范围见 [v49 视觉复查及桌面差异](frontend-acceptance-2026-09-14-v49.md)。

预览启动注意：8093 服务必须具有读取 Bestdori 的网络权限。2026-09-14 曾误从受限环境重启，导致图片代理 WinError 10013 / 502，页面原图可见但没有生成补全；已通过获准的联网执行重启。新启动后应检查未缓存活动的纹理 `ready`，不能以静态页可打开或图片可显示作为通过标准。v48 同时修复前景克隆为阴影前 Blob URL 被过早释放的问题。

- 正式入口为 `apps/web/design.css`，层级为 runtime → bindings → approved → plumbing。确认稿模板和样式独立保存在 `src/ui/approved/`，业务绑定与共享组件单独维护。
- 卡牌列表保留紧凑尺寸；排序只选择规则，升降序独立切换；“持有状态”以发布时间作为默认第二关键字。
- 最新浏览器视觉复查、桌面差异及未覆盖边界见 [2026-09-14 v49 验收记录](frontend-acceptance-2026-09-14-v49.md)。此前直接模板接入的实测记录见 [2026-09-13 验收记录](frontend-acceptance-2026-09-13.md)及 [2026-09-12 验收记录](frontend-acceptance-2026-09-12.md)。
- 本轮导入调整：97 项前端测试、16 项服务端测试通过，服务端编译检查通过；已在浏览器核对国服导入说明、配置来源切换及队伍导入的缺失 ID 提示。未使用真实游戏账号执行成功导入。此前 Rust 数据层 35 项通过、8 项已有测试忽略，Tauri 编译检查通过；这些历史检查不替代本轮桌面端交互验收。
- 正式功能提交包含相互依赖的前端、运行时、服务接口与回归测试；确认稿素材及部署验收文档分别提交。

如需重新生成确认稿，先准备包含 `boundary-design` 等页面的设计工作区，以及 Python 的 `beautifulsoup4`、`tinycss2`。再依次执行 `python scripts/import-design-markup.py <设计工作区>` 和 `python scripts/sync-approved-design.py <设计工作区>`，检查生成差异。普通构建使用仓库内已生成文件，不依赖本地设计工作区。

2026-09-11：用户授权“可以开始”后，将确认稿接入正式 apps/web，并补充 HTTP、Tauri 接口。保留原技术栈与求解器。未提交或发布版本。

设计依据：以设计工作区 **boundary-design 五页实际预览**及其最终加载的样式、DOM 和交互为准。接入清单仅用于核对功能，不能替代视觉验收。release-policy、language-policy、boundary-acceptance 约束真实数据行为。

## 本次 Git diff 审阅顺序（2026-09-14）

下表保留提交前的 141 个文件审阅快照，包括新源码、确认稿、图片和测试。

| 分组 | 文件数 | 审阅重点 |
| --- | ---: | --- |
| Rust 服务与桌面接口 | 7 | 头图 PNG 通道、按档案 ID 保存、原生文件保存；移除服务端国服旧导入依赖与路由 |
| 确认稿与游戏素材 | 26 | `ui/approved/` 模板及样式、三张独立页面背景、突破／稀有度／技能图标及来源 |
| 共享前端接入 | 79 | 五页布局、共用卡库和编辑器、活动／语言／日期规则、按档案隔离的偏好与历史、导入／导出及桌面适配 |
| 前端回归测试 | 10 | 新增 8 份测试，更新结果展示测试，删除已移除入口的旧测试；当前合计 101 项通过 |
| 开发与设计同步脚本 | 3 | 受限图片代理、确认稿 HTML/CSS 同步；构建直接使用仓库内生成文件 |
| 说明与部署文档 | 16 | 素材代理部署、账号模块保留说明、实际验收范围与未验证部分 |

提交组织为三组：确认稿与素材（含同步脚本，28 个文件）、正式功能接入（含测试，97 个文件）、部署与验收文档（16 个文件）。国服旧导入移除与新前端接入在 `actions/profile.js`、运行时适配器和服务端入口中交叉，归入同一笔功能提交。

提交前复核：101 项前端测试、16 项服务端测试、1 项头图路径约束测试通过，`git diff --check` 通过。此前桌面开发构建通过；原生截图工具报错和保存流程未实测的限制仍保留在 v49 验收记录中。

本次整理移出了未被当前页面引用的 `redesign.css`、`ui/player.css`、`cards/catalog.css` 和三张早期技能 SVG；保留 PNG 游戏图标与已确认的样式层级。移出的文件备份在 Git 忽略的 `tmp/git-diff-cleanup-20260914/`。三处仅换行符变化已清理；构建产物、预览日志、临时备份及真实配置未纳入 diff。

## 已接入

- 五页并列导航、B color 侧栏与正文内边距；全局语言按照所选语言、简中、繁中、日、英、韩去重回退。
- 活动 C 风格、嵌套计算参数、活动类型及演奏方式自动选择加成模型、D 难度标识、三队编辑与主乐队草稿导入。
- 卡库与选卡器共用筛选、分组、排序和卡片展示；默认当前服未来 168 小时内且持有，发布限制不约束持有、组队或计算。
- 共用简略卡、技能数值／类型图标、非零突破徽标、队长与技能顺序；可编辑详情与结果只读快照。切图不改变特训养成。
- 批量持有和养成、当前范围／分组全选、隐藏选择计数、恢复用养成备份、分批挂载。
- 紧凑真实道具图片，通用道具归属性分类，角色分项加成；指定装备及结果展示实际所选道具图片与等级。
- 原有四种计算入口、配置变化提示、每档案独立历史。旧格式历史仅在结果数据库清理。
- 档案目录与详情分离、真实养成概览、显式保存基本资料、切换使用。导入／导出／复制作用于目录中选中的档案，异步操作捕获目标档案；查看其他档案时提供切换入口。
- 所选活动完整背景、前景及 Logo 分层；Worker 轻量纹理补全。卡牌头图采用所有持有中最新、自动跳过 2 星、默认特训后、允许手动选择；其他页采用确认的独立背景。
- 空状态、失败提示、移动端计算与状态提示、弹窗固定操作区；失败不丢弃已有结果。

## 主要模块

- 外壳：apps/web/src/ui/shell.js、archive.js、activity.js、team-editor.js；样式入口 apps/web/design.css。
- 卡牌：apps/web/src/ui/cards/；正式适配 views/card-library.js。
- 道具／角色：views/player-library.js；样式 ui/approved/player.css 与 ui/fidelity.css。
- 头图：ui/hero.js、texture-worker.js；本地背景及来源在 apps/web/assets/headers/。
- 偏好及保存：ui/preferences.js、app/player.js、storage/user.js。
- 计算快照及历史：actions/calculation.js、data/result-cache.js。
- 素材通道：crates/data/src/hero_asset.rs；HTTP、Tauri、Python 开发服务器均提供受限入口。

## 验证

- 原前端基线 63 项；接入后 **82 项前端测试通过**。新增覆盖语言和发布边界、头图候选、未挂载全选、字段隔离、未知卡记录、真实技能等级、特训等级上限、保存队列、同步期间编辑及计算晚到时的档案归属。
- cargo test -p bangdream-optimize-data --features native-cache --lib：**35 通过，8 个原有慢速／诊断用例保持忽略**，含 SP 独立发布时间及自动选曲限时规则。
- cargo check -p bangdream-optimize-server：通过。
- cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml：通过。
- git diff --check：通过。

通过独立 127.0.0.1:8093 实例的实际界面操作，保留两份名称包含“验收”的样例档案。没有使用真实游戏账号凭据。

1. 15 张真实卡牌、39 个道具、突破 1–4：最大平均 PT、最高得分、三队巡回指定队伍均完成 WASM 计算。最高得分样例 5,306,863；三队指定样例平均分数 4,849,931。控分实际搜索完成，验证了无精确方案状态（1,000／5,000 PT 样例）。
2. 2,000 条持有记录：默认匹配 1,717 张、40 位角色；首次挂载 280 张，全选仍选中全部 1,717 张。搜索香澄后保留 1,660 张筛选外选择；再次全选取消当前 57 张，其余选择保留。未知资料独立显示，原始记录保留。
3. 新档案历史为空；切回原档案可恢复历史。查看及重命名另一档案，不改变正在使用的档案。
4. 全局语言切换保留搜索／筛选。结果临时切图不改档案插图偏好，也不让结果过期。
5. 结果详情只读，五星卡正确显示 60 / 60；队长、技能顺序和非零突破可同时呈现。
6. 五页 320、375px 无整页横向溢出；卡库、道具、档案 768px 通过；1280px 已视觉检查。960×420 队伍弹窗内容可滚动、底部操作可见。窄屏队伍局部横向滚动，长技能记号不挤压。
7. 真实活动 #341 可以手动选择，完整背景、前景与 Worker 纹理已加载；最终版本的三队指定计算完成，平均分数 5,695,732。当前可见图片无失败，最终浏览器错误日志为空。
8. 29 个 UI／页面模块通过 node --check 语法检查。

## 运行及实际验收范围

本次实例：python -X utf8 scripts/serve-web.py --port 8093，使用已有 apps/web/pkg 与 var/game-data。
生产额外反代 /bestdori/header/；详见 docs/web-static.md、docs/nginx-reverse-proxy.conf。

Tauri 已编译，未启动桌面窗口完成全部原生交互验收。真实游戏账号在线导入、邮件反馈发送和生产部署未执行；原有入口、格式处理与失败重试保留。浏览器测试使用真实游戏数据和合成养成记录，不代表这些外部服务已验收。


## 按实际预览重新对齐（2026-09-11）

本轮按照用户“和预览完全一致，而不是仅仅按照接入清单修改”的要求，重新连接最终预览的页面结构和样式。沿用正式的数据、档案存储、计算及发布规则；不引入 PreviewSession、演示数据或预览服务器依赖。

| 页面 | 对齐的结构及共用组件 |
| --- | --- |
| 外壳 | 218px 侧栏、五项并列导航、顶部语言／档案行、正文左侧内间距、确认的头图层次 |
| 活动 | 活动与目标 → 加成 → 演出设置 → 指定队伍 → 区域道具 → 歌曲；额外计算参数嵌入活动与目标；加成图标选择和模型切换 |
| 卡牌 | 中性筛选区、乐队淡色、圆形角色选择、密度切换、两行悬停工具栏、快速跳转、全宽分组底色、统一简略／详细卡牌 |
| 道具与角色 | 乐队折叠行、展开道具图架、属性与通用道具、杂志、角色矩阵编辑 |
| 结果 | 三曲指定队伍作为完整样例；消耗倍率、四项主指标、道具图片、唱片式歌曲信息、五卡队伍和得分范围；历史抽屉 |
| 档案与数据 | 目录及选中状态、养成概览、基本资料、图片服务器选项、资源分区、新建／复制表单、快速档案切换、导入变化核对 |

当时样式入口为 `apps/web/design.css`，按 legacy → integration → approved → bindings 分层；已被下述 2026-09-12 的直接模板接入替代。
`scripts/sync-approved-design.py <设计工作区绝对路径>` 可重新导入已确认样式，生成 `apps/web/src/ui/approved/`。`ui/fidelity.css` 只负责真实节点与预览节点之间的适配，以及移除遗留样式影响。共用卡牌展示和筛选继续集中维护。

本轮检查：五页真实浏览器切换，约 1400px 桌面与 400px 窄屏布局；五页均未出现页面横向滚动。验证指定队伍编辑、加成两种模型字段互斥、道具草稿取消、真实三曲历史结果恢复、档案新建取消、非当前档案选择。修复了被移除标题的空引用、浮点百分比阻断表单、隐藏页面中的跨页弹窗容器问题。前端 82 项测试通过，修改的 JS 均通过语法检查。

页面数字、档案名、语言回退和卡牌内容由真实资料决定，不能使用预览中的固定样例数字。网页端与桌面端资源动作仍遵守真实能力。此次视觉回归在网页端进行；此前 Rust/Tauri 构建验证不等于本轮桌面端实机视觉验证。未发送游戏账号导入请求、提交反馈、发布或提交 Git 变更。

## 直接模板接入（2026-09-12，版本 29）

按用户要求直接复用最终稿 HTML/CSS。`scripts/import-design-markup.py` 生成静态片段、动态模板、侧栏和档案流程 SVG；与 `scripts/sync-approved-design.py` 使用同一个设计工作区参数。生成物及来源 SHA256 位于 `apps/web/src/ui/approved/`。

正式样式入口不再加载 `styles.css`、`redesign.css`，层级为 runtime → bindings → approved → plumbing。原稿样式优先；真实数据绑定、共享卡牌组件、档案存储和 WASM 求解保留。新增、导入、导出和批量养成也使用源模板，移除预览模拟选项。

已重新实测四种计算、1717 张筛选结果的全选、队伍/歌曲/道具编辑、非零突破卡牌、档案导出导入新档案完整流程、五页手机布局和低高度弹窗。90 项测试通过；具体数字、截图目视范围及未覆盖的外部服务见 `docs/frontend-acceptance-2026-09-12.md` 文末。预览入口：`http://127.0.0.1:8093/?design=29#activity`。
