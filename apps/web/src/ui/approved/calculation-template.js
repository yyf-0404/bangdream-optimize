// Generated verbatim from preview-calculation-settings.js.
export function calculationMarkup({state,server,m,t,l,R}) {
 const escape=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const value=path=>path.split('.').reduce((o,k)=>o[k],state);
 function field(label,path,{unit='',step=1}={}){return `<label class="calc-field"><span>${label}</span><span class="calc-input"><input type="number" min="0" step="${step}" required data-setting="${path}" value="${escape(value(path))}" aria-label="${label}">${unit?`<span>${unit}</span>`:''}</span></label>`;}
 function choice(label,path,options){return `<fieldset class="calc-choice"><legend>${label}</legend><div class="pc-choice-row">${options.map(([v,n])=>`<label class="pc-choice"><input type="radio" name="calc-${path}" data-setting="${path}" value="${v}" ${String(value(path))===String(v)?'checked':''}><span>${n}</span></label>`).join('')}</div></fieldset>`;}
 function select(label,path,options){return `<label class="calc-field"><span>${label}</span><select data-setting="${path}" aria-label="${label}">${options.map(([v,n])=>`<option value="${v}" ${String(value(path))===String(v)?'selected':''}>${n}</option>`).join('')}</select></label>`;}
 const rank=Array.from({length:5},(_,i)=>[i,`第 ${i+1} 名`]);
 function teammateFields(){const p=state.ptMaximize,count=p.teammateMode==='uniform'?1:4;return `<div class="calc-roster"><div class="calc-roster-head" aria-hidden="true"><span>适用玩家</span><span>预期综合力</span><span>队长技能加分</span><span>技能持续时间</span></div>${Array.from({length:count},(_,i)=>`<div class="calc-player"><strong>${count===1?'四位队友共用':`队友 ${i+1}`}</strong>${field('预期综合力',`ptMaximize.teammates.${i}.expectedStat`)}${field('队长技能加分',`ptMaximize.teammates.${i}.leaderScoreUp`,{unit:'%',step:'any'})}${field('技能持续时间',`ptMaximize.teammates.${i}.leaderSkillDuration`,{unit:'s',step:'any'})}</div>`).join('')}</div>`;}
const p=state.ptMaximize;
  let body='';
  if(m==='ptMaximize'){
   body=choice('演奏方式',`ptMaximize.liveVariantByEventType.${t}`,(R.variants[t]||[]).map(v=>[v,R.names[v]]));
   if(l==='cooperative')body+=`<div class="calc-block"><div class="calc-block-title"><h5>自身与队友</h5><span>共 5 人 · 队友参数用于估算平均 PT</span></div><div class="calc-grid">${field('自身综合力下限','ptMaximize.minimumPersonalStat')}${choice('队友参数','ptMaximize.teammateMode',[['uniform','统一填写'],['individual','分别填写']])}</div>${teammateFields()}</div><div class="calc-block"><h5>第六次技能</h5><div class="calc-grid">${select('队长选择方式','ptMaximize.cooperativeLeaderMode',[['max_stat','综合力最高'],['specified','指定玩家'],['random','随机玩家']])}${p.cooperativeLeaderMode==='specified'?select('使用谁的队长技能','ptMaximize.cooperativeSpecifiedLeader',[[0,'自己'],...Array.from({length:4},(_,i)=>[i+1,`队友 ${i+1}`])]):''}</div><p class="calc-note">${{max_stat:'综合力最高的玩家提供技能；并列时随机选择。',specified:'固定使用指定玩家的队长技能作为第六次技能。',random:'在自己和四位队友中等概率随机选择。'}[p.cooperativeLeaderMode]}</p></div>`;
   if(l==='versus')body+=`<div class="calc-block calc-grid">${select('个人排名','ptMaximize.versusTeamRank',rank)}</div>`;
   if(l==='festival')body+=`<div class="calc-block"><div class="calc-grid">${select('队内个人排名','ptMaximize.festivalTeamRank',rank)}${choice('队伍结果','ptMaximize.festivalWon',[[true,'获胜'],[false,'未获胜']])}</div></div><div class="calc-block"><div class="calc-block-title"><h5>队友预期分数</h5><span>另外 4 位玩家</span></div>${choice('填写方式','ptMaximize.festivalTeammateMode',[['uniform','统一填写'],['individual','分别填写']])}<div class="calc-grid calc-scores">${Array.from({length:p.festivalTeammateMode==='uniform'?1:4},(_,i)=>field(p.festivalTeammateMode==='uniform'?'每位队友':`队友 ${i+1}`,`ptMaximize.festivalTeammateScores.${i}`)).join('')}</div></div>`;
   if(t==='mission_live')body+=`<div class="calc-block">${field('任务支援 PT 加成','ptMaximize.missionSupportPtBonus',{unit:'%'})}</div>`;
   if(['solo','challenge_cp','medley'].includes(l))body+=`<p class="calc-note">${l==='challenge_cp'?'使用 CP 挑战规则；CP 消耗在结果页选择。':l==='medley'?'按三曲连续演出计算；每曲火耗在结果页选择。':'按自由演出计算平均 PT；火耗在结果页选择。'}</p>`;
  }else{
   body=`<div class="calc-target"><div class="calc-grid">${field('当前 PT','scoreRange.currentPt')}${field('目标总 PT','scoreRange.targetTotalPt')}</div><p class="calc-delta" aria-live="polite"></p></div><div class="calc-grid">${choice('Auto 基础倍率','scoreRange.autoBaseMultiplier',[[0.5,'× 0.50'],[0.75,'× 0.75']])}${t==='mission_live'?field('任务支援 PT 加成','scoreRange.missionSupportPtBonus',{unit:'%'}):''}</div><p class="calc-note">${server==='jp'?'日服默认 × 0.75':'当前服务器默认 × 0.50'}，可按实际版本调整。</p><p class="calc-instruction"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="m8 5 11 7-11 7Z"/></svg><span>${t==='medley'?'使用巡回演出的自动演出，在第一首歌曲结束后退出。':'使用自由演出的自动演出。'} 系统匹配歌曲与火耗，给出一组控分方案。候选仅含当前服已发布的歌曲与难度，SP 按独立发布时间判断，限时歌曲不参与。</span></p>`;
  }
  return `<div class="calc-heading"><h4 id="calc-settings-title" class="bo-subheading"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" aria-hidden="true"><path d="M4 7h7m4 0h5M4 17h3m4 0h9M11 4v6M7 14v6"/></svg>${m==='ptMaximize'?'最大平均 PT 参数':'控分设置'}</h4><span>${m==='ptMaximize'?'平均 PT':'Auto · 精确目标'}</span></div><div class="calc-body">${body}<p class="calc-error" role="status" hidden></p></div>`;

}
